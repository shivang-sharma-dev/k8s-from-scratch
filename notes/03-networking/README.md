# 03 — Networking

Covers how traffic flows inside and outside the cluster.

## Topics
- **Services** — ClusterIP, NodePort, LoadBalancer
- **Ingress** — HTTP/HTTPS routing and host-based rules
- **NetworkPolicies** — pod-level firewall rules
- **DNS** — service discovery inside the cluster via CoreDNS
