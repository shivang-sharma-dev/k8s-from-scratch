# 05 — Configuration

Covers how to inject config and secrets into pods.

## Topics
- **ConfigMaps** — non-sensitive key/value config data
- **Secrets** — sensitive data (base64 encoded), env or volume mount
- **Resource Requests & Limits** — CPU/memory guarantees and caps
- **Environment Variables** — injecting values directly into containers
